/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['i.imgur.com', 'imgs.search.brave.com', 'www.reddit.com' ,'styles.redditmedia.com']
    }
};

export default nextConfig;
